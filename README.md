# py-native-io

Native-first asynchronous kernel I/O for Python targeting `io_uring` (Linux), `IoRing` (Windows), and `kqueue` (macOS/BSD).
See `PROJECT_GOALS.md` for full architectural specifications and platform roadmap.