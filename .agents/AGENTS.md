# Workspace Agent Guidelines

- **Testing Standard**: Any Python or Rust code written, modified, or refactored in this repository MUST be tested using the appropriate testing framework (`pytest`, `maturin`, `cargo test`, `mypy`, `ruff`). Refer to `.agents/skills/testing-python-rust/SKILL.md` for execution rules.
